<!--pages/components/loading/loading.wxml-->
<view class="animation-loading {{nightModel ? 'night' : ''}}" wx:if="{{status}}">
  <view class="loading">
    <view class="round"></view>
    <view class="round"></view>
    <view class="round"></view>
    <view class="round"></view>
  </view>
</view>
